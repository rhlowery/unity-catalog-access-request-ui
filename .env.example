# Frontend Environment (Vite)

# URL of the Backend-For-Frontend (BFF) server.
# In development, this points to the local Express server.
# In production, update this to your deployed BFF URL.
VITE_BFF_URL=http://localhost:3001
